# 比赛版本状态

**更新日期**：2026-08-09
**AI 负责人**：AI Competition Owner
**当前结论**：工程版已稳定通过隔离自动彩排；尚未冻结，比赛电脑真实服务彩排、安全密钥与固定 Git 提交仍是最后门槛。

## 当前质量基线

- Top 20 RAG 首条准确率：**20/20（100%）**，发布门槛 90%。
- 后端自动测试：**12/12 通过**，覆盖会话、游客额度、权限、模型故障、断库管理员回退和向量一致性。
- 隔离浏览器彩排：**10/10 通过**；每轮验证新游客首次回答、第二问登录、强管理员会话、草稿自动续问且只发送一次。
- 社区知识闭环：浏览器已验证“候选生成→管理员审核→再次问答出现标注社区来源”。
- 现场 HTTP 冒烟：**4/4 通过**，覆盖健康检查、匿名管理拦截、LightRAG 管理代理拦截和跨域凭证。
- Web 构建：主 JS 386.77 kB（gzip 139.50 kB），无 500 kB 警告；生产包会忽略误配的本机 API 地址并走同源代理。
- 移动端 390 px：登录异步加载成功，页面和弹窗越界元素 0。
- 知识资产：396 条正式资料、408 条切片、本地 `hash-256` 索引 408/408；Top 20 仍以关键词检索作为稳定兜底。

## 已完成

1. AI 比赛负责人、单一状态文件、14 天路线、7 分钟主演示和备用问题池。
2. 游客首次问答、第二次登录、草稿保留、登录后自动续问。
3. 签名会话、HttpOnly 游客 Cookie、失败不扣额度、管理员接口保护。
4. LightRAG → 本地检索 → 模型失败保留官方来源的三层回退。
5. MySQL 断开时显式强管理员内存回退；注册关闭，社区内存审核知识仍可参与 RAG。
6. 一键质量预检、HTTP 冒烟、移动端布局检查、10 轮浏览器彩排和机器可读证据。
7. 普通预检与严格冻结验收分离，避免“带警告可演示”被误报为“已冻结”。

## 尚未冻结的原因

1. 比赛环境尚未配置至少 10 位管理员强密码、32 位以上会话密钥和游客 Cookie 密钥。
2. 仍需在比赛电脑用真实 DeepSeek、MySQL、TTS 和比赛网络执行一次完整 7 分钟脚本。
3. 当前工作区尚未形成固定比赛提交和干净回滚点。
4. 在线 BAAI/bge-m3 真实向量仍需单独的数据外传授权；未授权时使用自描述本地哈希索引，不会误调用在线服务。

## 下一步唯一重点

在比赛电脑配置密钥并完成一次真实服务彩排；确认成功后设置 `COMPETITION_LIVE_REHEARSAL_CONFIRMED=true`，创建固定提交，再运行严格冻结验收。

## 固定命令

```powershell
npm run competition:check          # 日常一键预检，可带明确 WARN
npm run competition:smoke          # 后端启动后的实时 HTTP 冒烟
npm run competition:rehearse       # 10 轮隔离浏览器彩排，不调用外部模型/TTS
npm run competition:freeze:check   # 严格冻结门槛
```
