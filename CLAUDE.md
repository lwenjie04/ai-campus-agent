# CLAUDE.md — Prism 项目工作规范

## 用户画像

用户不是技术人员，描述需求时可能出现以下情况：
- 用日常语言描述，不用技术术语
- 需求可能模糊、不完整、前后矛盾
- 看到不符合预期会直接说"太丑了""完全没用""你到底在做什么"
- 情绪表达直接，不耐烦时语气会变差

**这不是用户的错。** 用户脑子里有画面，只是无法用技术语言表达。

## 核心工作原则

### 1. 产品经理思维

每次收到需求，先翻译成技术语言：

```
用户说："这个跑马灯为什么不动"
↓ 产品经理翻译
需求：页面中的标签条需要自动滚动动画
问题：当前 CSS animation 未生效（可能是 scoped 样式或 keyframes 问题）
方案：改用 JS requestAnimationFrame 驱动滚动
```

### 2. 先确认，再动手

- 用户说"修一下XX"，先确认他看到的和你理解的是同一个东西
- 给出选项让他选，而不是猜一个就改
- 大改动前先给方案，小改动直接修

### 3. 每次只修一个区域

- 把页面拆成独立区域，逐个修
- 修完一个确认好了再修下一个
- 不要一次改 5 个文件然后全坏掉

### 4. 改完立即验证

- 改完就跑 `npx vite build` 确认编译通过
- 编译不过 = 没改完，不要停下来
- 改完跑 `npx codegraph index` 更新索引

### 5. 解释你改了什么

- 每次改动后用简单的语言解释
- 不用技术术语，用"你看到的：xxx"的方式
- 让他知道改了哪里、为什么要改

## 项目信息

- **名称**：Prism（校园 AI 知识助手）
- **技术栈**：Vue 3 + TypeScript + Vite + Element Plus + Pinia
- **路径**：`D:/VUE3-GDES-Agent/ai-campus-agent/`
- **启动**：`npm run dev` → `http://localhost:5173/`
- **构建**：`npx vite build`

## 每次开始工作前

1. 运行 `npx codegraph status` 了解项目状态
2. 确认要修改哪个文件、哪个区域
3. 告诉用户你要改什么，一句话说清楚

## 每次修改完成后

1. 运行 `npx vite build` 确认编译通过
2. 运行 `npx codegraph index` 更新索引
3. 告诉用户改了什么，用他能看懂的话
